# Hunt 资产浏览器 · 计划表（理论上限评估）

目标终态：打开即所见即所得的"游戏内观感"预览 + 一键丢进 Unity/VRChat。
下面的"上限"是按开源格式资料 + 已验证样本给的诚实评估，不是许愿。

## 阶段与上限

| 阶段 | 内容 | 状态 | 理论上限 |
|---|---|---|---|
| 1 | 目录树浏览、模型点看、骨架 | ✅ 已交付 | — |
| 2 | wem 音频在线转码播放 | ✅ 已交付 | — |
| 3 | 贴图预览（分裂 DDS: BC1/3/5/7)、cdf 整模装配、武装骨骼挂点、猎人编号套装、Shift 叠加拼装、mtl/dds 查看器 | ✅ 已交付(v1.2~demo) | — |
| 4 | **完整材质预览**: diffuse + 法线(ddna) + 高光(spec) + 光泽(gloss), sRGB 正确, 双灯+环境简单 PBR | 🔜 下一个 | 游戏静态观感 ~85%。法线是 BC5 双通道+alpha 分片重组, 例程都验证过, 是体力活不是风险 |
| 5 | **glTF(.glb) 一键导出**: 几何+骨架+材质+贴图打包, Unity/Blender/VRChat SDK 直接认 | 🔜 与 4 同批 | 静态模型导出 100% 可做; VRChat avatar 需用户自己绑 PhysBone/表情(不在范围内) |
| 6 | **动画播放**: 右栏动作名可点播, dba/caf 关键帧解码 → 蒙皮实时驱动骨架 | 待评估(中高风险) | 单条 bspace/caf 播放可行(Markemp v2 已实现过, 格式可参考); bspace 混合树完整复现不承诺 |
| 7 | ↑↓ 方向键速切同目录模型 / 动画条目 | 简单, 随 6 一起 | — |
| 8 | **音效关联反推**: animevents 的参数=Wwise 事件ID → FNV 哈希反查 wem 文件名 → 动作对应音效可调 | 待评估(中风险) | 需要验证 Hunt 的 wwise 命名哈希; 成功则"动作 → 音效"可查, 失败则只能按目录人工浏览(现状兜底) |

## 已知边界（上限到不了的地方）

- **着色器 1:1 复现不可能**: 游戏内效果还混了皮肤次表面散射(SSS)、毛发各向异性、自发光、
  程序化污渍遮罩等 CryEngine 私有 shader。我们最多近似到"双灯+材质通道贴满"。真 1:1 要用游戏引擎本身。
- **elite/皮肤动态变体**(游戏内变色、特效层)是运行时逻辑, 静态文件里只有基础配置。
- **物理披风/布料摆动**是运行时模拟, 静态导出没有。

## 已完成格式清单（解密/解析都通）

pak 加密(RSA+AES+zip64) ✓ / cgf skin chr 骨骼+几何+子材质 ✓ / skinm cgfm 半精度几何 ✓
mtl 文本 XML + SubMaterials ✓ / 分裂 DDS(148B stub + .N 分片, cross-pak) ✓ /
BC1 BC3 BC5 BC7 解码 ✓ / cdf 附件+骨挂点变换 ✓ / chrparams + animevents 清单 ✓ /
wem → ogg (ww2ogg 双码书) ✓ / hunter 换装编号表 ✓

## 选型记忆（为什么自己写而不是照抄大佬）

- Noesis + CryEngine 插件: 最接近"带 shader 直开", 但闭源预览器, 塞不进我们的一键 Linux 链。
- Markemp Cryengine-Converter: C#, 转 glTF 走 Blender 再看; 我们拿它当格式字典用(IntSkinVertices
  布局等), 它的输出路线重, 不适合浏览器直看。
- 结论: 联网扒格式定义 + 本地命名统计反推, 混合路线, 已被证明是对的。
